// import listUserRolesByUserId from 'boredom-lib/services/user-roles/list-by-userid'
// import listRequestMapsByUrl from 'boredom-lib/services/request-map/list-by-url'
// import matchUrl from 'boredom-lib/services/request-map/match-url'
// import { resolve as i18n } from 'boredom-lib/i18n'
// import UnauthorizedError from 'boredom-lib/errors/api/unauthorized'

export const handler = async (user, method, url) => {
  // const requestMaps = await listRequestMapsByUrl(url, { limit: 100 })
  // const userRoles = await listUserRolesByUserId(user.id, { limit: 100 })
  // if (!userRoles.length) throw new UnauthorizedError(i18n('User has no roles configured'))
}
