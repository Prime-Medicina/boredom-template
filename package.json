{
  "name": "boredom-template",
  "version": "0.0.1",
  "scripts": {
    "wizard": "npx lerna-wizard",
    "deploy": "lerna exec --scope sls-* sls deploy",
    "deploy:init": "lerna exec --scope sls-resources-* sls deploy && lerna exec --scope sls--* sls deploy",
    "deploy:resources": "lerna exec --scope sls-resources* sls deploy",
    "deploy:api": "lerna exec --scope sls-functions-api-* sls deploy"
  },
  "devDependencies": {
    "@babel/core": "^7.11.5",
    "babel-eslint": "^10.1.0",
    "babel-jest": "^26.3.0",
    "eslint": "^7.8.1",
    "eslint-config-airbnb-base": "^14.2.0",
    "eslint-config-prettier": "^6.11.0",
    "eslint-plugin-import": "^2.22.0",
    "eslint-plugin-prettier": "^3.1.4",
    "lerna": "^3.22.1",
    "prettier": "^2.1.1",
    "serverless": "^1.80.0",
    "serverless-bundle": "^3.0.0"
  }
}
